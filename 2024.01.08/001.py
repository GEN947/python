# primitive 타입 -> import
# 값 1개
    # 숫자 -> 정수(int), 실수(float)
    # 문자열 -> str
    # 참/거짓 -> bool
# sequence
    # 순서대로 읽고 쓴다: list, tuple
    # [175, 165, 180]       (175, 165, 180)
    # list는 변경 가능, tuple은 변경 불가능

    # 계산으로 위치 파악: set 
    # {175, 165, 180}

    # 키와 값의 쌍(pair): dictionary 
    # {"홍길동": 175, "전우치": 165, "임꺽정": 180}
    # list + list (하나는 키 / 다른 하나는 값)

'''       오류가 있음 2024.01.08 - 006.py 확인          '''