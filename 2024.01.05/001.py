# primitive type: 원시타입(기본타입) / import 없이 기본으로 쓸 수 있는 type ...

# 값 1개: int, float, str, bool
# collection(값을 여러개 가질 수 있는것): list, tuple, set, dictionary

# Create Read Update Delete(CRUD)       리스트에 값을 만든다 / 읽는다 / 변경한다 / 삭제한다
# list: 변경 가능
# tuple: 변경 불가능

[1,2,3]
(1,2,3)
{1,2,3}
# 이름과 값(key와 value)의 쌍
{
    "name":"홍길동",
    "age":20,
}

# sequence 쓰면 for는 무조건 쓴다고 생각하면 된다.